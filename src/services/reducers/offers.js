import { offer } from "../../utils/offer";
const initialState = offer;

const offerList = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
export default offerList;